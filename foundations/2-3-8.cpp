#include <iostream>
#include <vector>

/*
2.3-8: Two Sum Problem
*/

/*
PSEUDO CODE
two_sum(S, x)
    SORT(S)
    left = 1
    right = S.length
    while left < right
        sum = S[left] + S[right]
        if sum == x
            return TRUE
        else if sum > x
            right = right - 1
        else
            left = left + 1
    return FALSE
*/

int two_sum() {
    return 0;
}

int main() {
    return 0;
}